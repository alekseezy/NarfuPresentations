<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="NarfuPresentations.Presentation.MAUI.Pages.DialogPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewModels="clr-namespace:NarfuPresentations.Presentation.MAUI.ViewModels"
    xmlns:views="clr-namespace:NarfuPresentations.Presentation.MAUI.Pages"
    Title="{Binding Dialog.Sender}"
    x:DataType="views:DialogPage">

    <CollectionView ItemsSource="{Binding Dialog.Messages}" VerticalOptions="StartAndExpand">
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="viewModels:DialogViewModel">
                <Border
                    Margin="8,8,8,0"
                    Padding="0"
                    Style="{StaticResource RoundedSqueareWidgetWithShadow}">
                    <Grid>
                        <Frame
                            Padding="0"
                            BackgroundColor="{AppThemeBinding Light={StaticResource BackgroundWhite},
                                                              Dark={StaticResource SecondDark}}"
                            CornerRadius="10"
                            HorizontalOptions="Fill">

                            <Label Text="{Binding .}" />

                        </Frame>
                    </Grid>
                </Border>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>


</ContentPage>